# C Programming Tutorials

This repository contains **basic C programming tutorials**  
Each program focuses on a fundamental concept of the C language such as input/output, conditions, loops, and basic logic building.

## 📁 Programs Included

- **area of circle.c**  
  Calculates the area of a circle using a given radius.

- **grade.c**  
  Determines a student’s grade based on marks.

- **multiplication table.c**  
  Prints the multiplication table of a given number.

- **name age country.c**  
  Displays user details such as name, age, and country.

- **negative positive num.c**  
  Checks whether a number is positive, negative, or zero.

- **number day.c**  
  Converts a number (1–5) into the corresponding day of the week.

- **random guess.c**  
  A simple number guessing game using random numbers.

- **sum 1 to num.c**  
  Calculates the sum of numbers from 1 to a given number.

- **sum num.c**  
  Finds the sum of digits of a number.

## 🛠️ How to Compile and Run

Use a C compiler like **GCC**:

```bash
gcc filename.c -o output
./output
